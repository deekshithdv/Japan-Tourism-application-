<div class="collapse navbar-collapse" id="navbarSupportedContent">
  <ul class="navbar-nav me-auto mb-2 mb-lg-0"> 
  </ul>
  <%= form_tag(search_page_path, :method => "get") do %>  
  <div class="input-group">  
    <%= search_field_tag :search, params[:search], placeholder: "Search category", class: "form-control" %>  
    <%= button_tag "Search", :class => 'btn btn-outline-success',:name => nil, style: "margin-left:2px;" %> 
  </div>  
<% end %>
  <div class="dropdown">
  <i class="fa fa-lg fa-user-o dropbtn"></i> 
  <div class="dropdown-content">
  <%= link_to "Profile", profile_path(current_user.id) %>
  <%= link_to "Logout" , destroy_user_session_path, method: :delete %>
  </div>
</div>
  <a class="nav-item">
  </a>
</div>